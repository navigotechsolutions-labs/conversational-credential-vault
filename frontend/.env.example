# Core Vault Frontend Configuration

# URL pointing to the Core Vault backend API server.
# Fallback when empty or unset: relative path '/api' (useful when routed through proxy/Nginx)
VITE_API_URL=http://localhost:8333/api
